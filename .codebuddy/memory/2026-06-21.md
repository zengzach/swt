# 2026-06-21 工作日志

## GitHub MCP Server 配置与修复

### 问题排查
- GitHub MCP Server 在 `~/.codebuddy/mcp.json` 中已配置（`@modelcontextprotocol/server-github`），但服务器启动即崩溃。
- 根因：npx 缓存 `/Users/zach/.npm/_npx/3dfbf5a9eea4a1b3` 中 `zod` 模块缺失（`ERR_MODULE_NOT_FOUND: Cannot find module '.../zod/index.js'`）。
- 修复：删除损坏的 npx 缓存目录，重新 `npx -y @modelcontextprotocol/server-github` 预安装，服务器正常启动（`github-mcp-server v0.6.2`）。
- 修复后需重启 CodeBuddy 才能加载 MCP 服务器。

### 注意事项
- `@modelcontextprotocol/server-github@2025.4.8` 已被 npm 标记为 deprecated（不再支持），但仍可正常使用。
- 该包不提供 `get_me` 工具；可用工具包括 `search_repositories`、`get_file_contents`、`list_issues`、`create_repository`、`create_branch` 等 26 个工具。
- GitHub Personal Access Token 存储在 mcp.json 明文中。

## 创建 GitHub 仓库
- 使用 `create_repository` 工具创建了公开仓库 `zengzach/swt`。
- URL: https://github.com/zengzach/swt
- 已初始化 README.md，默认分支 main。
